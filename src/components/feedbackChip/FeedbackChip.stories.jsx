import FeedbackChips from ".";



export default {
  title: 'component/FeedbackChips',
  component: FeedbackChips
};

const Template = args => <FeedbackChips {...args} />

export const Default = Template.bind({});

Default.args = {
}
