import { Avatar, Chip, styled } from '@mui/material';

export const StyledAvatar = styled(Avatar)(() => ({
        background: "#007C32",
        '&.MuiChip-avatarColorDefault': { color: "#fff" }

    }))

export const StyledFeedbackChip = styled(Chip)(() => ({
    }))